data(std::io)

data main() {
    data obj = "Hello!"
    print(obj)
    data a: data[9] = 30
    data b = 30
    data sum = add(a,b)
    data ptr: data& = &(sum!data64)
    print(ptr)
}

data x86_64-apple-darwin$add(data, data) : data {
    mov rax, rdi
    add rsi, rax
    ret
}